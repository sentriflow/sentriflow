{
  "name": "sentriflow",
  "private": true,
  "type": "module",
  "version": "0.1.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:cli": "bun build packages/cli/index.ts --compile --env=disable --outfile sentriflow.exe",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "version": "node scripts/bump-version.mjs"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.27.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
